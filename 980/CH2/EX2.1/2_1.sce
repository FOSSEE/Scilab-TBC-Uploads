clc;
clear;
format('v',4)                                                                   //formating the number display
h=(log(0.9))/(-0.0001193);                                                      //calculating the height 
disp(h,'The height h at which the pressure is 9/10 of the pressure at the surface of the earth is (in meter):');                                                //displaying the height 
