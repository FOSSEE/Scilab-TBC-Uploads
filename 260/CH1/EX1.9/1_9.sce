//Eg-1.9    
//pg-22

clear
clc

a=input("enter any number")
if a<=0 then
    disp("logarithm cannot be computed")
exit(0);
end
    p=log(a);
    disp("logarithm of given number is ")
    disp(p)
