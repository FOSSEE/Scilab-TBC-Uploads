                //Example 7.4
//determine the larger of two quantities

function[] =  maximum(x, y)
    if (x >=y) then z=x; else z=y;
    end
    printf("\n\nMaximum value %d", z);
    return;
endfunction